<IfModule mod_headers.c>
    # Versionierte Assets – unbegrenzt cachen (URL ändert sich bei jedem Deploy)
    <FilesMatch "\.(js|css|wasm|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|json)$">
        Header set Cache-Control "public, max-age=31536000, immutable"
    </FilesMatch>
</IfModule>
